# goKV
A concurrent, in-memory key-value database built from scratch in Go. Features a thread-safe storage engine, native RESP protocol parsing, hybrid AOF/snapshot persistence, and real-time Pub/Sub messaging. Fully compatible with standard redis-cli.
